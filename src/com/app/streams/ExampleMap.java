package com.app.streams;

public class ExampleMap {
	public static void main(String[] args) {
		/*List<Student> students = Arrays.asList(new Student("Andrés"), new Student("Juan"), new Student("Gaviria"), new Student("Suárez"));
		List<String> names = students.stream().map(a -> a.getName()).collect(Collectors.toList());
		System.out.println(names);
		
		List<Student> students2 = names.stream().map(n -> new Student(n)).collect(Collectors.toList());
		System.out.println(students2);
		*/
	}
}
